# n8n Stack - Umgebungsvariablen
# Kopiere als .env und trage echte Werte ein

# PostgreSQL Passwort (fuer n8n + n8n-db)
N8N_DB_PASSWORD=DEIN_SICHERES_PASSWORT

# n8n Encryption Key (fuer Credentials-Verschluesselung)
N8N_ENCRYPTION_KEY=DEIN_ENCRYPTION_KEY
