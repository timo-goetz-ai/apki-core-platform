apiVersion: v1
kind: Secret
metadata:
  name: "{{ project_name | default('chatbot') }}-secrets"
  namespace: "{{ project_name | default('chatbot') }}"
type: Opaque
stringData:
  OPENAI_API_KEY: "replace-me"
