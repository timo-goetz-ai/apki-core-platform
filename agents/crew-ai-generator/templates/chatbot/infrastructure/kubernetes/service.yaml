apiVersion: v1
kind: Service
metadata:
  name: {{ project_name | default('chatbot') }}-svc
  namespace: {{ project_name | default('chatbot') }}
spec:
  type: ClusterIP
  selector:
    app: {{ project_name | default('chatbot') }}
  ports:
  - name: http
    port: 80
    targetPort: 8501
